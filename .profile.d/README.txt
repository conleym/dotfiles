Shell functions, &c. for both bash and zsh.


* base.sh contains basic functions to be sourced directly from .profile.

* scm_breeze.sh loads scm breeze. Usually best loaded from
  shell-specific init files.

* addons/ contains everything else and should be loaded from
  shell-specific init files.
