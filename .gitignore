# Ignore the repo itself.
.cfg/

# Contain history, passwords, other sensitive crap that shouldn't be
# tracked.
*history*   # various history files for shells and apps.
.lesshst    # less history
.pgpass     # postgresql usernames & passwords
.wget-hsts  # wget known hosts database

# Local repo contains bunch of jars. Maven config can contain
# passwords.
.m2

# Ignore pip log and cache, but track config.
.pip/*
!.pip/pip.conf

# OpenSSL random seed.
.rnd
